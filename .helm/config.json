{
	"$comment": "Per-repo run config. checkCommand must be one simple command: no shell operators (&&, ||, ;, |, &) and no commas, because compound commands and commas break the run's tool allowlist.",
	"checkCommand": "pnpm check"
}
