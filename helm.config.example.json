{
	"$comment": "Where the daemon finds each repo. The repo's own run config (Auto-allowlist override, check command) lives in its .helm/config.json.",
	"repos": [
		{
			"path": "/absolute/path/to/target-repo",
			"mainBranch": "main"
		}
	]
}
