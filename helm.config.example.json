{
	"$comment": "checkCommand is optional and must be one simple command: no shell operators (&&, ||, ;, |, &) and no commas. Compound commands and commas break the run's tool allowlist.",
	"repos": [
		{
			"path": "/absolute/path/to/target-repo",
			"mainBranch": "main",
			"checkCommand": "pnpm check"
		}
	]
}
